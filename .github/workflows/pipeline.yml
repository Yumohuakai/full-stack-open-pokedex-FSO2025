name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm install
      - name: Check style
        run: npm run eslint
      - name: Build for production
        run: npm run build
      - name: Test - Jest
        run: npm run test
      - name: E2E-test - 1.Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: E2E-test - 2.Run Playwright tests
        run: npm run test:e2e
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      - name: Trigger deployment
        run: curl https://api.render.com/deploy/srv-${{ secrets.RENDER_SERVICE_ID }}?key=${{ secrets.RENDER_API_KEY }}
        if: ${{ github.event_name == 'push' }}   

  tag_release:
    needs: [simple_deployment_pipeline]
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'push' }}
    permissions:
      contents: write
    steps:
      # - name: Test print built-in GITHUB_TOKEN way 1 # works
      #   run: |
      #     echo "${{ secrets.GITHUB_TOKEN }}" 
      # - name: Test print built-in GITHUB_TOKEN way 2 # works
      #   run: |
      #     echo "${{ github.token }}" 
      # - name: Test print built-in GITHUB_TOKEN way 3 # does not work
      #   run: |
      #     echo "$GITHUB_TOKEN" 
      # - name: Test print env
      #   run: |
      #     echo "${{ github.repository }} - $GITHUB_REPOSITORY" 
      # - name: Bump version and push tag way 1 # does not work
      #   uses: anothrNick/github-tag-action@1.73.0
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #     DEFAULT_BUMP: patch
      - name: Bump version and push tag way 2
        # the 'github-tag-action' is currently updated at 1.73.0
        # here uses the hash code instead of version number: 1.73.0 for the third-party actions        # 
        uses: anothrNick/github-tag-action@e528bc2b9628971ce0e6f823f3052d1dcd9d512c
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DEFAULT_BUMP: patch
          DRY_RUN: true